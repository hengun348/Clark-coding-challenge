<ul class="questionnaires">
	{{#each model as |questionnaire|}}
		<li class="questionnaires__item">
			{{#link-to "about" questionnaire.id}}
				{{questionnaire.title}}
			{{/link-to}}
		</li>
	{{/each}}
</ul>